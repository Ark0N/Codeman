[2026-09-15] Chose in-app split-pane sessions (two live terminal panes + draggable divider) over the pre-existing detachSession()/isSoloWindow (separate OS window) approach — detach already covers "two sessions visible at once" but not one Codeman window with a divider you can drag without leaving your seat.
[2026-09-15] Chose Pane B (SplitTerminalPane) deliberately plainer than Pane A — no local-echo overlay, no CJK IME, no touch/mobile handlers, no keyboard accessory bar — because split-pane is desktop-only by nature (needs a wide viewport) and those subsystems exist for mobile/touch input.
[2026-09-15] Chose to patch CodemanApp.prototype._onSessionDeleted (and later .selectSession for the self-split guard) at module-evaluation time in terminal-split.js, never the live instance inside a DOMContentLoaded listener — connectSSE() in app.js caches the handler function reference once on first connect, so an instance-level or delayed patch would silently never fire.
[2026-09-15] Rejected patching app.js/terminal-ui.js directly anywhere in this feature — hard constraint carried through all 7 tasks and the final-review fix wave. Zero deletions across the whole branch; verified in the final review.
[2026-09-16] Final whole-branch review found 2 Critical + 6 Important issues the per-task reviews couldn't see individually (most notably: .btn-split--hidden had no CSS rule at all, so the "opt-in default-OFF" Split button shipped visible to every user). Bundled into one fix wave per SDD rules (no per-finding fixers), then one scoped re-review — which itself found a new listener-leak regression in the picker-dismiss fix, fixed in a follow-up commit and independently re-verified.
[2026-09-16] Chose to deploy this branch as the standard production server via a systemd override repoint (codeman-web.service.d/override.conf now points ExecStart at this worktree's dist/index.js) rather than merging into tlkMods — reasons: (1) trivial instant rollback (repoint the override back, no git operations), (2) tlkMods is the shared main checkout other Codeman sessions actively work against, and merging would move HEAD out from under them, (3) keeps this branch's history clean for an eventual PR against master, uncontaminated by whatever else lands on tlkMods while this is being dogfooded. Original override backed up to codeman-web.service.d/override.conf.bak-pre-split-pane. CODEMAN_DATA_DIR unchanged (~/.codeman) — all existing sessions/state carried over untouched. PR against master intentionally deferred until after real-world use.
